import React, { useEffect, useState } from 'react';
import styled from 'styled-components'

const Gomoku = ({ className }) => {

  return (
    <div>Gomoku</div>
  );
}

export default styled(Gomoku)`
  
`;
