import React, { useEffect, useState } from 'react';
import styled from 'styled-components'

const Sudoku = ({ className }) => {

  return (
    <div>Sudoku</div>
  );
}

export default styled(Sudoku)`
  
`;
