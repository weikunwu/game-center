{
    "cacheDirectories": [
      "webapp/node_modules"
    ],
    "engines": {
        "node": "17.x"
    },
    "scripts": {
      "heroku-postbuild": "cd webapp && npm install && npm run build && cp -R ./src/assets ./build/assets"
    }
  }